import { asyncHandler } from "../middlewares/asyncHandler.middleware";

// export const sellerDashboardStats = asyncHandler(async(req,res,next) => {
//  const sellerId = req.user.id;

//  const stats = await Promise.all([
  
//  ])
// })